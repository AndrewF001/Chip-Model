#include "TestClass.h"
/*
bool Test_Function();

class adder : public Block {
public:
	bool entryFunction() { return true; }
};

bool Test_Function() {
	Chip c;
	ChipInputStream s;
	// empty
	c.execute(s);

	// 1 block
	auto b = std::make_unique<adder>();
	c.addBlock(std::move(b));
	c.execute(s);
	return true;
}
*/

const bool TestClass::AllTests() {
	return true;
}
